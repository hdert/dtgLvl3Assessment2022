include links.pug

mixin header(activePage)
  // This header, the entirety of <head> and the footer is based on a template and injected into
  // the webpage when it is transpiled and packed (efficient tools and techniques).
  // This transpilation and packing is done by Parcel (efficient tools and techniques #2).
  // Use css-flex to center everything, set width to 100% to fill up the page
  .header-image.w-100.align-items-center.justify-content-center.d-flex
    // Add padding and margins for spacing, aesthetics (C.R.A.P.).
    .hero-text-container.p-5.mx-3
      // center text, make text bold, center
      h1.hero-text.align-middle.m-0.fw-bold.text-center What Lies Beneath
  // Make navbar 
  nav.navbar.navbar-expand.navbar-dark.bg-primary.sticky-top.container-fluid 
    #navbarContent.collapse.navbar-collapse.justify-content-center
      .navbar-nav 
        // The following three links are generated by a pug mixin (efficient tools and techniques).
        +nav-link("index", activePage, "Home")
        +nav-link("subscribe", activePage, "Updates")
        +nav-link("artists", activePage, "Artists")
    button.navbar-toggler(
      type="button",
      data-bs-toggle="collapse",
      data-bs-target="#navbarContent",
      aria-controls="navbarContent",
      aria-label="Toggle Drop-Down Navigation",
      aria-expanded="false"
    )
      span.navbar-toggler-icon
